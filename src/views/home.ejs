<%- include('components/head', { title: 'Home' }) %>

    <h1>Home</h1>
    <% images.forEach(img => { %>
        <img src="/uploads/<%= img %>" style="max-width: 300px; max-height: 300px;" />
    <% }) %>

<%- include('components/footer', { title: 'Home' }) %>